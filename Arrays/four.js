
// join() method
// it converts array into csv format(Comma seperated value)

const arr=["A","B","C","D","E","F","G"];

const newValue=arr.join();
console.log(newValue);

// Split() method
// it converts csv format to Array format

const string = "A,B,C,D,E,F,G"
const newArray= string.split(",");
const newArray1= newValue.split(",");
console.log(newArray)
console.log(newArray1)
