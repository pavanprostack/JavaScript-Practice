// grade

let score = 70;
let collegeStudent = true;

let myGrade = score >= 90 ? "A" 
              :score >= 80 ? "B" 
              :score >= 70 ? "C" 
              :score >= 60 ? "D" 
              : collegeStudent ? "U" :"F"

console.log(myGrade)              